// Place tests here.

suite('tests', () => {
  test('test', () => {
    assert(null, 'place some tests here');
  });
  test('ok', () => {
    assert(true, 'ok');
  });
  test('ok', () => {
    assert(true, 'ok');
  });
  test('not ok', () => {
    assert(false, 'not ok');
  });
});

suite('tests2', () => {
  test('test', () => {
    assert(null, 'place some tests here');
  });
  test('ok', () => {
    assert(true, 'ok');
  });
  test('ok', () => {
    assert(true, 'ok');
  });
  test('not ok', () => {
    assert(false, 'not ok');
  });
});
